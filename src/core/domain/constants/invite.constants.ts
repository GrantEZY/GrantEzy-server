export enum InviteStatus {
    CREATED = "CREATED",
    ACCEPTED = "ACCEPTED",
    REJECTED = "REJECTED",
    SENT = "SENT",
}

export enum InviteAs {
    TEAMMATE = "TEAMMATE",
    REVIEWER = "REVIEWER",
}
